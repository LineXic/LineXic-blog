{
    "buildCommand": "pnpm run build",
    "outputDirectory": "./dist",
    "installCommand": "pnpm install",
    "redirects": [
      {
        "source": "$host",
        "destination": "$wwwhost",
        "statusCode": 301
      }
    ]
}